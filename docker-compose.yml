
services:

  kuberion-ai:

    build: .

    container_name: kuberion-ai

    ports:
      - "7860:7860"

    env_file:
      - .env

    volumes:
      - ./data:/app/data

    restart: unless-stopped